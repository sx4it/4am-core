= form_for @machine , html: { class: 'form-horizontal'} do |f|

  -if @machine.errors.any?
    .control-group
      .alert.alert-error
        %a.close{href: "#", data:{ dismiss:'alert' }} ×
        %h4.alert-heading= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
        %ul
          - @user.errors.full_messages.each do |msg|
            %li
              %p= msg
  .control-group
    = f.label :name, class: 'control-label'
    .controls
      = f.text_field :name
  .control-group
    = f.label :ip, class: 'control-label'
    .controls
      = f.text_field :ip
  .control-group
    = f.label :machine_tpl_id, class: 'control-label'
    .controls
      = f.text_field :machine_tpl_id
  .form-actions
    = f.submit 'Save', class: "btn btn-info"
