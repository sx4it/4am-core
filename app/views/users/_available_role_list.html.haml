#AvailableRoleList
  %ul
  - @roles.each do |r|
    - unless @user.roles.include? r
      %li
        = link_to r.name + ' +',{:id => @user, :role => r, :action => :add_role}, :remote => true, :method => :post, class: 'label label-success'
