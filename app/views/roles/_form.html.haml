- include_related_js

#add_role_form

  = simple_form_for @new_role, html: { class: 'form-horizontal' } do |f|

    = f.input :roles, :input_html => {:class => :span3, :multiple => true}, :as => :text

    .form-actions.transparent
      = f.submit 'Save', class: "btn btn-info"
