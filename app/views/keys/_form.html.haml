
= form_for @key, html: { class: 'form-horizontal' } do |f|
  -if @key.errors.any?
    .control-group
      .alert.alert-error
        %a.close{href: "#", data:{ dismiss:'alert' }} ×
        %h4.alert-heading= "#{pluralize(@key.errors.count, "error")} prohibited this key from being saved:"
        %ul
          - @key.errors.full_messages.each do |msg|
            %li
              %p= msg
  
  .control-group
    = f.label :name, class: 'control-label' 
    .controls
      = f.text_field :name, class: 'input-xxlarge'
      %p.help-box This is only used to help you remember which key it is.

  .control-group
    = f.label :ssh_key, class: 'control-label' 
    .controls
      = f.text_area :ssh_key, class: 'input-xxlarge'
      %p.help-box You can add key by copy-pasting it or by importing a file from your local filesystem. The format of the key is
      %p.help-box
        %code ssh-[type] AAAAB3NzaC1yc2E... name?

  .control-group
    = f.label :ssh_key, class: 'control-label' 
    .controls
      = f.file_field :ssh_key, class: 'input-file'

  .form-actions
    = f.submit 'Save', class: "btn btn-info"

